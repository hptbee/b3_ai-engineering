# plan

**Action now:** produce an implementation plan; do not write product code unless the user also asked to implement.

**Skill:** `skills/engineering/planning/SKILL.md`

Also load `skills/architecture/simplest-correct/SKILL.md` if the plan chooses system shape.

Host mapping (slash command vs Cursor command) belongs in `adapters/`.
