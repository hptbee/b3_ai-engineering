# Volatile knowledge

Technology-specific, fast-changing notes belong here (React, Next.js, TypeScript, Node.js, Azure, Cursor, Codex, Claude Code, and similar).

No entries yet. When added, include `last_verified`, `source`, and `version` where appropriate. Stale entries are hints to re-check, not facts.
