# Skill evals

Activation cases per skill:

```text
<skill>/
├── should-trigger/
├── should-not-trigger/
└── near-miss/
```

Capability (quality) fixtures can sit beside those buckets when they exist.

No cases yet. First candidates: the four foundation skills under `skills/engineering/`.
