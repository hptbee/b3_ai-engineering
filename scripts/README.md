# Scripts

Deterministic helpers for skills, evals, or adapters.

**Status:** none yet. Do not add scripts until a mechanical step is painful to improvise (eval runners, report formatters, adapter generators).

Prefer portable shell, Node, or Python. Host-only wrappers belong under `adapters/`.
